import { Container } from "@material-ui/core";
import { FC } from "react";

const Done: FC = () => {
  return (
    <Container component="main" maxWidth="xs">
      <p>Done!</p>
    </Container>
  );
};

export default Done;
