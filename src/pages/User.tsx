import { Container } from "@material-ui/core";
import { FC } from "react";

const User: FC = () => {
  return (
    <Container component="main" maxWidth="xs">
      <p>User page</p>
    </Container>
  );
};

export default User;
